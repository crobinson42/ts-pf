# @ts-pf/server

`createImplementer()`, middleware, `runProcedure`, `lookupProcedure`, `createLocalClient()`, and server call interceptors / `DedupePlugin`. No `Request` / `Response`.

Agent skill: [`skills/ts-pf-server/`](skills/ts-pf-server/). Sync with `npx skills experimental_sync -y`.

HTTP serving is [`@ts-pf/server-http`](../server-http). See the [root README](../../README.md) for the Fetch happy path.
