# @ts-pf/contract

`procedure` / `router`, schema adapters, nested routers, and infer types. No HTTP, no runtime.

Agent skills: [`skills/ts-pf-contract/`](skills/ts-pf-contract/) and hub catalog [`skills/ts-pf-app/`](skills/ts-pf-app/) (Fetch happy path plus opt-in capabilities). Sync with `npx skills experimental_sync -y`.

See the [root README](../../README.md) for the Fetch happy path.
