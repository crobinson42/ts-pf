# @ts-pf/protocol

Portable JSON envelope, `PFError`, and `PROTOCOL_VERSION`. No HTTP server, no schemas, no codecs.

Agent skill: [`skills/ts-pf-protocol/`](skills/ts-pf-protocol/). Sync with `npx skills experimental_sync -y`.

Wire spec: [`PROTOCOL.md`](PROTOCOL.md).
