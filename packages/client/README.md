# @ts-pf/client

`createClient()`, `Link`, `intercept()` / `CallPlugin`, `RetryPlugin` / `DedupePlugin` / `CachePlugin`, `asResult()`, and `isLocalFailure()`. No Fetch.

Agent skill: [`skills/ts-pf-client/`](skills/ts-pf-client/). Sync with `npx skills experimental_sync -y`.

HTTP calling is [`@ts-pf/client-http`](../client-http). See the [root README](../../README.md) for the Fetch happy path.
