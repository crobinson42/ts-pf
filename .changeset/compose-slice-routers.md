---
"@ts-pf/server": minor
"@ts-pf/contract": patch
---

Nest slice contracts and finished implementations with `router({ planet: planetContract })` and `impl.router({ planet: planetApp })`. Procedure `path` follows the composed tree. Each server still exports one contract for clients.
